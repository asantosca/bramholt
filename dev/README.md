# dev/

The codebase(s) live here. **Placeholder** — no stack chosen yet.

When you're ready to build:
1. Decide the stack (record it in `company/decisions/` as a new ADR).
2. Scaffold the app in a subfolder, e.g. `dev/app/`.
3. Give it its **own** `.gitignore` and toolchain config — keep build noise out of the
   company workspace.
4. Add a `dev/app/CLAUDE.md` with run/build/test commands so Claude can work the code.

Until then this folder is intentionally empty (kept in git via `.gitkeep`).
